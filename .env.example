# Optional: Gemini API Key for AI Executive Briefings (built-in intelligent summarizer works even if omitted)
GEMINI_API_KEY=

# Optional: Bearer token for securing /api/cron/sync-news against unauthorized triggers
CRON_SECRET=pulse_secret_super_secure_key_12345
